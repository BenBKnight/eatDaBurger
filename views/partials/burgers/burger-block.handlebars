<li>
	{{burger_name}}

	<button class="devour" data-id="{{id}}" data-newdevour="{{eaten}}">
		{{#if eaten}}Devour!{{else}}Order!{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">DELETE!</button>
</li>
